# Rewind promo video

A 30-second, 1920×1080 Remotion promo for Rewind. The composition is built from vector and CSS elements, so it is fully editable and does not depend on external image assets.

```bash
npm install
npm run preview
npm run render
```

The final H.264 video is written to `out/rewind-promo.mp4`.
